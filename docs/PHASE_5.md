<div dir="rtl">

# تحویل فاز ۵ — ورودی صوتی

## خلاصه
- کنار کادر «افزودن سریع» (در برنامه، ویجت و کاشی تنظیمات سریع) و در صفحهٔ دستیار، دکمهٔ میکروفون اضافه شد.
- **روش استفاده:**
  - دکمه را بزن و به فارسی بگو، مثلاً «فردا ساعت ۵ عصر به مامان زنگ بزنم».
  - وقتی ساکت شوی، ضبط خودش تمام می‌شود. برای تمام کردن زودتر، دوباره دکمه را بزن.
  - متن در همان کادر قرار می‌گیرد تا قبل از ثبت ببینی و ویرایش کنی.
- **حریم خصوصی:**
  - تبدیل صدا به متن کاملاً روی خود گوشی با whisper.cpp انجام می‌شود.
  - صدا هیچ‌جا فرستاده یا ذخیره نمی‌شود. فایل موقت بلافاصله پس از تبدیل پاک می‌شود.

## مدل‌های تشخیص گفتار

### نسخهٔ اول: مدل‌های اصلی Whisper
در نسخهٔ اول از مدل‌های اصلی Whisper استفاده شده بود. آزمایش روی گوشی نشان داد دقت فارسی‌شان خیلی ضعیف است.

### ارزیابی (`tools/speech_eval` و workflow «Speech eval (Persian)»)
- **داده:** ۳۰ جملهٔ فارسی واقعی از مجموعهٔ عمومی FLEURS (fa_ir).
- **اجرا:** با کد تشخیص گفتار خود برنامه، روی ۴ هستهٔ پردازنده.
- **معیارها:** نرخ خطای کلمه (WER) و نرخ خطای حرف (CER).

| مدل | حجم | WER | CER | زمان هر جمله (~۱۰ ثانیه) |
|---|---|---|---|---|
| Whisper Base اصلی | ۶۰ مگابایت | ۸۵٪ | ۳۰٪ | ۱٫۳ ثانیه |
| Whisper Small اصلی (نسخهٔ اول) | ۱۹۰ مگابایت | ۶۰٪ | ۲۰٪ | ۸٫۷ ثانیه |
| Whisper Turbo اصلی | ۵۷۴ مگابایت | ۳۰٪ | ۶٫۶٪ | ۲۵ ثانیه |
| **Base فارسی (C1Tech)** ← پیش‌فرض | **۶۰ مگابایت** | **۳۰٪** | **۵٫۳٪** | **۲٫۲ ثانیه** |
| Small فارسی (AmirMohseni) | ۱۹۰ مگابایت | ۲۵٪ | ۷٫۲٪ | ۷٫۸ ثانیه |
| Turbo فارسی (nezamisafa) ← گزینهٔ گوشی‌های قوی | ۵۷۴ مگابایت | ۱۹٪ | ۵٫۶٪ | ۲۵ ثانیه |

- **جست‌وجوی پرتو (beam) و متن راهنما:** بهبود محسوسی نداشتند. تنظیم سریع (greedy) حفظ شد.
- **چرا WER بیشتر از CER است:** بخش زیادی از خطای کلمه‌ای به فاصله و نیم‌فاصله برمی‌گردد (مثل «می‌شناختند» در برابر «میشناختند»)، نه به خود واژه.

### انتشار
- مدل‌های فارسی در CI از Hugging Face به فرمت whisper.cpp تبدیل و فشرده می‌شوند. این کار با workflow «Publish speech models» انجام می‌شود.
- فایل‌ها در Release «speech-models» همین مخزن منتشر شده‌اند و برنامه از همان‌جا دانلود می‌کند.
- مجوزها:
  - Base فارسی: Apache-2.0
  - Turbo فارسی: MIT
- مدل‌های قدیمی دیگر برای تشخیص استفاده نمی‌شوند. با زدن میکروفون، دانلود مدل فارسی پیشنهاد می‌شود. مدل قدیمی را می‌توانی از صفحهٔ مدل‌ها حذف کنی.

## معماری
- **whisper.cpp v1.9.4:**
  - به‌صورت زیرماژول اضافه شد.
  - روی همان ggml ِ llama.cpp کامپایل می‌شود. در APK فقط یک نسخه از ggml وجود دارد و انتخاب خودکار نسخهٔ پردازنده برای هر دو موتور کار می‌کند.
- **پروسهٔ `:ai`:** تشخیص گفتار در همان پروسهٔ دستیار اجرا می‌شود. صدا از طریق یک فایل موقت (PCM خام ۱۶ کیلوهرتز) به سرویس می‌رسد، چون binder برای آرایه‌های بزرگ محدودیت دارد.
- **ضبط:**
  - `AudioRecord` با منبع VOICE_RECOGNITION، ۱۶ کیلوهرتز و تک‌کاناله.
- **تشخیص سکوت (`Vad`، Kotlin خالص):**
  - سطح نویز محیط را یاد می‌گیرد، پس در محیط پرسروصدا صدای بلندتری لازم است.
  - صحبت را از ۱۵۰ میلی‌ثانیه صدای پیوسته تشخیص می‌دهد.
  - بعد از ۱٫۲ ثانیه سکوت تمام می‌کند.
  - اگر تا ۸ ثانیه کسی حرف نزند، لغو می‌کند.
  - حداکثر طول ضبط ۳۰ ثانیه است.
- **پیش از تبدیل،** سکوت ابتدا و انتهای صدا بریده می‌شود.
- **دقت واژه‌های خاص:** نام پروژه‌ها و برچسب‌های کاربر به‌عنوان «متن راهنما» به whisper داده می‌شود تا این واژه‌ها بهتر تشخیص داده شوند.
- **پاک‌سازی خروجی:**
  - برچسب‌هایی مثل [موسیقی] حذف می‌شوند.
  - ی و ک عربی به فارسی تبدیل می‌شوند.
  - نقطهٔ پایانی حذف می‌شود.
- **ماژول جدید `feature:voice`:** دکمهٔ `VoiceButton` قابل استفاده در هر صفحه. شامل اجازهٔ میکروفون، انیمیشن سطح صدا و حالت‌های گوش دادن و تبدیل.

## تست‌ها
- **VAD:**
  - پایان بعد از مکث
  - مکث کوتاه وسط صحبت
  - نبود صحبت
  - محیط پرسروصدا
  - سقف طول
- **PCM/WAV:** رفت و برگشت فایل، بریدن سکوت.
- **پاک‌سازی متن خروجی.**
- **CI (workflow «Assistant eval»، کار speech):** کد تشخیص گفتار خود برنامه با هر سه مدل روی ۴ هستهٔ پردازنده اجرا و زمان‌گیری می‌شود.

### زمان تبدیل در CI (نسخهٔ اول، مدل‌های اصلی)
نمونه: صدای انگلیسی ۱۱ ثانیه‌ای خود whisper.cpp، روی ۴ هستهٔ پردازنده.

| مدل | زمان تبدیل | متن |
|---|---|---|
| Base | ۱٫۸ ثانیه | کاملاً درست |
| Small | ۶٫۳ ثانیه | کاملاً درست |
| Large v3 Turbo | ۲۰٫۱ ثانیه | کاملاً درست |

- جمله‌های کوتاه برنامه‌ریزی معمولاً ۳ تا ۵ ثانیه‌اند و تبدیلشان به همان نسبت سریع‌تر است.
- روی گوشی احتمالاً ۱٫۵ تا ۳ برابر کندتر است.

## محدودیت‌ها
- دقت فارسی whisper، به‌ویژه Base، کامل نیست. متن همیشه قبل از ثبت نمایش داده می‌شود و پارسر زمان روزبان خطاهای کوچک را جبران می‌کند.
- ارزیابی روی جمله‌های خوانده‌شدهٔ عمومی است، نه دستورهای کوتاه برنامه‌ریزی. دقت روی جمله‌های خودت را آزمایش روی گوشی نشان می‌دهد.
- خواندن متن با صدا (TTS) در این فاز نیست.

</div>
