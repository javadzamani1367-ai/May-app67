# سامانه مدیریت بازدید مراکز رمزارز

مستند فنی پروژه — مرجع اصلی توسعه. این فایل را در ریشه پروژه قرار دهید تا Claude Code در هر جلسه آن را بخواند.

---

## ۱. هدف پروژه

سامانه‌ای برای مدیریت چرخه کامل بازدید از مراکز استخراج رمزارز در استان ایلام: از لحظه دریافت گزارش تا تکمیل مستندات، تولید گزارش رسمی و ارسال مدارک به واحدهای سازمانی.

**دو نرم‌افزار:**
- اپ اندروید — ابزار میدانی کارشناسان (اولویت اول ساخت)
- نرم‌افزار ویندوز — آرشیو مرکزی و گزارش‌گیری (فاز دوم)

---

## ۲. محدودیت‌های قطعی

این محدودیت‌ها قابل مذاکره نیستند و انتخاب فناوری را تعیین می‌کنند:

| محدودیت | پیامد |
|---|---|
| نرم‌افزار دسکتاپ باید روی **ویندوز ۷** نصب شود | Electron نسخه ۲۳ به بعد، Tauri و Flutter Desktop حذف می‌شوند |
| **بدون سرور** — همه‌چیز روی گوشی و یک ویندوز مرکزی | معماری آفلاین‌اول، همگام‌سازی مستقیم دستگاه به دستگاه |
| **حجم کم** | کتابخانه‌های سنگین ممنوع، ML Kit کنار گذاشته شد |
| کار در مناطق **بدون اینترنت** | هیچ قابلیتی نباید به شبکه وابسته باشد |
| متن **فارسی راست‌به‌چپ** در همه خروجی‌ها | موتور تولید PDF باید RTL را کامل پشتیبانی کند |

---

## ۳. پشته فناوری

### اپ اندروید
```
زبان:            Kotlin
رابط کاربری:      Jetpack Compose + Material 3
حداقل نسخه:       Android 8.0 (API 26)
هدف:             API 34
پایگاه داده:      Room روی SQLite + SQLCipher
دوربین:          CameraX
بارکدخوان:       ZXing Android Embedded  (حدود ۴ مگابایت سبک‌تر از ML Kit)
موقعیت:          FusedLocationProviderClient
وب‌سرور:          NanoHTTPD  (حدود ۵۰ کیلوبایت)
تاریخ شمسی:      پیاده‌سازی داخلی PersianDate  (بدون وابستگی خارجی)
تولید PDF:       WebView + PrintDocumentAdapter  (بومی اندروید، حجم صفر، RTL بی‌نقص)
تولید Word:      ساخت مستقیم OOXML به صورت زیپ  (بدون کتابخانه)
حجم هدف APK:     زیر ۲۰ مگابایت
```

### نرم‌افزار ویندوز (فاز دوم)
```
پلتفرم:          .NET Framework 4.8  (روی ویندوز ۷ SP1 نصب می‌شود)
رابط کاربری:      WPF
پایگاه داده:      SQLite + SQLCipher  (همان اسکیمای اندروید)
تولید PDF:       QuestPDF  (مجوز MIT، پشتیبانی RTL)
تولید Word:      DocumentFormat.OpenXml
تولید Excel:     ClosedXML
حجم هدف نصاب:    زیر ۱۵ مگابایت
```

**نکته مهم برای تولید PDF در اندروید:** گزارش را به صورت HTML با CSS بساز، فونت وزیرمتن را به صورت base64 داخل خود HTML جاسازی کن، سپس با `WebView.createPrintDocumentAdapter()` به PDF تبدیل کن. این روش راست‌چینی، اعداد فارسی و شکست خط فارسی را بدون هیچ کتابخانه خارجی درست انجام می‌دهد.

---

## ۴. ساختار پروژه

```
crypto-inspection/
├── CLAUDE.md                  ← همین فایل
├── android/
│   └── app/src/main/java/ir/ilam/inspection/
│       ├── data/
│       │   ├── db/            ← Room: موجودیت‌ها، DAOها، پایگاه داده
│       │   ├── model/         ← مدل‌های دامنه و enumها
│       │   └── repo/          ← لایه ریپازیتوری
│       ├── ui/
│       │   ├── theme/         ← رنگ، تایپوگرافی، فونت وزیرمتن
│       │   ├── pending/       ← صفحه در دست اقدام
│       │   ├── intake/        ← ثبت سریع گزارش
│       │   ├── visit/         ← تکمیل میدانی (چندمرحله‌ای)
│       │   ├── archive/       ← بایگانی و جستجو
│       │   ├── dispatch/      ← ارسال مدارک به واحدها
│       │   └── settings/      ← تنظیمات و همگام‌سازی
│       ├── export/            ← تولید PDF و Word و Excel
│       ├── sync/              ← وب‌سرور و بسته‌ساز
│       └── util/              ← تاریخ شمسی، مهر تصویر، فشرده‌سازی
└── windows/                   ← فاز دوم
```

---

## ۵. اسکیمای پایگاه داده

اسکیما در اندروید و ویندوز **کاملاً یکسان** است. هر تغییری باید در هر دو اعمال شود.
مرجع ماشینی این اسکیما برای سمت ویندوز: `windows/SCHEMA.md`

### جدول `reports` — پرونده بازدید

| ستون | نوع | توضیح |
|---|---|---|
| `id` | TEXT PK | UUID — هرگز شماره ترتیبی نباشد |
| `tracking_code` | TEXT UNIQUE | کد رهگیری نهایی |
| `temp_code` | TEXT | کد موقت، اگر پرونده بدون شماره اشتراک ثبت شده |
| `report_type` | INTEGER | ۱ تا ۶، جدول بخش ۶ |
| `status` | INTEGER | ۰ در دست اقدام / ۱ بازدید شده / ۲ بایگانی |
| `expert_code` | TEXT | کد کارشناس ثبت‌کننده |
| `report_date` | INTEGER | تاریخ دریافت گزارش، میلی‌ثانیه یونیکس |
| `visit_date` | INTEGER | تاریخ بازدید، تهی تا زمان بازدید |
| `created_at` | INTEGER | |
| `updated_at` | INTEGER | برای همگام‌سازی افزایشی |
| `synced_at` | INTEGER | آخرین ارسال موفق به ویندوز |
| **مکان** | | |
| `county` | TEXT | شهرستان |
| `district` | TEXT | بخش |
| `address` | TEXT | آدرس کامل |
| `postal_code` | TEXT | |
| `latitude` | REAL | |
| `longitude` | REAL | |
| `gps_accuracy` | REAL | دقت موقعیت به متر |
| `file_number` | TEXT | شماره پرونده |
| `bill_number` | TEXT | شماره قبض |
| `subscription_number` | TEXT | شماره اشتراک |
| `usage_type` | TEXT | نوع کاربری |
| **مالک** | | |
| `owner_name` | TEXT | |
| `owner_national_id` | TEXT | |
| `owner_phone` | TEXT | |
| `owner_relation` | TEXT | نسبت با محل |
| **فنی** | | |
| `meter_amperage` | REAL | آمپراژ کنتور |
| `measured_amperage` | REAL | آمپراژ مصرفی اندازه‌گیری‌شده |
| `connection_type` | TEXT | نوع انشعاب |
| `seal_status` | TEXT | وضعیت مهر و موم |
| **شرح** | | |
| `description` | TEXT | شرح بازدید |
| `actions_taken` | TEXT | اقدامات انجام‌شده |

### جدول `devices` — دستگاه‌های استخراج

| ستون | نوع |
|---|---|
| `id` | TEXT PK |
| `report_id` | TEXT FK → reports.id |
| `row_number` | INTEGER |
| `model` | TEXT |
| `serial_number` | TEXT |
| `power_watt` | REAL |
| `entry_method` | INTEGER — ۰ بارکد / ۱ دستی |
| `note` | TEXT |

### جدول `attendees` — حاضرین در محل

| ستون | نوع |
|---|---|
| `id` | TEXT PK |
| `report_id` | TEXT FK |
| `organization` | INTEGER — ۰ شرکت برق / ۱ پلیس امنیت / ۲ سایر نهاد |
| `full_name` | TEXT |
| `position` | TEXT |
| `org_name` | TEXT — برای گزینه سایر |

### جدول `media` — تصاویر و ویدیو

| ستون | نوع |
|---|---|
| `id` | TEXT PK |
| `report_id` | TEXT FK |
| `type` | INTEGER — ۰ تصویر / ۱ ویدیو |
| `file_path` | TEXT — مسیر نسبی، هرگز مطلق |
| `caption` | TEXT |
| `captured_at` | INTEGER |
| `latitude` / `longitude` | REAL |
| `size_bytes` | INTEGER |

### جدول `attachments` — مدارک بعد از بازدید

| ستون | نوع |
|---|---|
| `id` | TEXT PK |
| `report_id` | TEXT FK |
| `category` | INTEGER — جدول بخش ۹ |
| `title` | TEXT |
| `file_path` | TEXT |
| `mime_type` | TEXT |
| `added_at` | INTEGER |
| `note` | TEXT |

### جدول `dispatches` — سوابق ارسال مدارک

| ستون | نوع |
|---|---|
| `id` | TEXT PK |
| `report_id` | TEXT FK |
| `unit` | INTEGER — ۰ فروش / ۱ حراست / ۲ حقوقی / ۳ برق شهرستان |
| `included_items` | TEXT — JSON آرایه شناسه‌ها |
| `note` | TEXT |
| `output_format` | INTEGER — ۰ PDF / ۱ Word |
| `dispatched_at` | INTEGER |

### جدول `settings`
کلید-مقدار ساده: کد کارشناس، کد ناحیه پیش‌فرض، آدرس مقصد همگام‌سازی، کیفیت رسانه.

---

## ۶. انواع گزارش و کد رهگیری

| کد | نوع گزارش | حرف | منبع کد رهگیری |
|---|---|---|---|
| ۱ | سامانه سراغ | — | از خود سامانه، دستی وارد می‌شود |
| ۲ | سامانه ۱۲۱ | N | ساخت خودکار |
| ۳ | گزارش مردمی | M | ساخت خودکار |
| ۴ | گزارش همکار | H | ساخت خودکار |
| ۵ | گزارش توانیر | T | ساخت خودکار |
| ۶ | بازدید میدانی | F | ساخت خودکار |

### قالب کد ساخته‌شده
```
[حرف نوع]-[کد ناحیه]-[YYMMDD شمسی]-[۶ رقم آخر شماره اشتراک]

نمونه:  M-۰۱-۰۵۰۶۱۴-۴۸۲۹۱۷
```

### کد موقت
اگر شماره اشتراک هنوز معلوم نیست، به جای شش رقم آخر از `T` به‌علاوه شماره ترتیبی چهاررقمی روزانه استفاده کن:
```
M-۰۱-۰۵۰۶۱۴-T۰۰۰۳
```
به‌محض ثبت شماره اشتراک، کد نهایی ساخته و در `tracking_code` نوشته می‌شود و کد موقت در `temp_code` باقی می‌ماند تا سابقه گم نشود.

### شهرستان‌های استان ایلام و کد ناحیه

| کد | شهرستان | | کد | شهرستان |
|---|---|---|---|---|
| ۰۱ | ایلام | | ۰۷ | سیروان |
| ۰۲ | ایوان | | ۰۸ | چرداول |
| ۰۳ | آبدانان | | ۰۹ | چوار |
| ۰۴ | بدره | | ۱۰ | ملکشاهی |
| ۰۵ | دره‌شهر | | ۱۱ | مهران |
| ۰۶ | دهلران | | ۱۲ | هلیلان |

کدهای ناحیه بالا پیش‌فرض هستند و باید در تنظیمات قابل ویرایش باشند تا با کدبندی رسمی شرکت توزیع هماهنگ شوند.

---

## ۷. فرم گزارش بازدید

خروجی PDF و Word از این هفت بخش ساخته می‌شود:

**بخش ۱ — مشخصات پرونده:** کد رهگیری، نوع گزارش، تاریخ گزارش، تاریخ بازدید، نام و کد کارشناس، وضعیت پرونده

**بخش ۲ — مشخصات مکان:** شهرستان، بخش، آدرس، کد پستی، شماره پرونده، شماره قبض، شماره اشتراک، نوع کاربری، مختصات GIS با دقت

**بخش ۳ — مشخصات مالک یا متصرف:** نام و نام خانوادگی، کد ملی، تلفن، نسبت با محل

**بخش ۴ — مشخصات فنی:** آمپراژ کنتور، آمپراژ مصرفی اندازه‌گیری‌شده، نوع انشعاب، وضعیت مهر و موم

**بخش ۵ — فهرست دستگاه‌ها:** جدول با ستون‌های ردیف، مدل، شماره سریال، توان (وات)، روش ثبت

**بخش ۶ — حاضرین در محل:** جدول با ستون‌های ردیف، نام و نام خانوادگی، سمت، سازمان

**بخش ۷ — شرح بازدید و اقدامات:** متن شرح، متن اقدامات، فهرست مستندات پیوست، جای امضا و مهر

**پیوست تصاویر:** بعد از بخش هفتم، تصاویر با شرح، تاریخ شمسی و مختصات درج‌شده روی هر تصویر.

---

## ۸. صفحات و جریان کاربری

### ناوبری اصلی — سه تب پایین صفحه
```
[ در دست اقدام ]   [ بازدید شده ]   [ بایگانی ]
```
به‌علاوه دکمه شناور «+» برای ثبت سریع گزارش جدید.

### تب در دست اقدام
قلب برنامه. هر پرونده یک کارت با:
- کد رهگیری (درشت)
- برچسب رنگی نوع گزارش — هر نوع یک رنگ ثابت
- آدرس مختصر و شهرستان
- شمارنده «۱۲ روز از ثبت گذشته» — بالای هفت روز نارنجی، بالای پانزده روز قرمز

مرتب‌سازی پیش‌فرض: قدیمی‌ترین بالا، تا پرونده‌های معطل دیده شوند.

### ثبت سریع (Intake)
فرم کوتاه: نوع گزارش، شهرستان و بخش، آدرس، شماره اشتراک (اختیاری)، تاریخ گزارش. با ثبت، کد رهگیری ساخته و پرونده وارد صف «در دست اقدام» می‌شود.

### تکمیل میدانی (Visit) — چندمرحله‌ای
پنج مرحله با نوار پیشرفت بالای صفحه:
۱. تأیید و تکمیل مکان + ثبت مختصات GIS
۲. مشخصات مالک
۳. مشخصات فنی و آمپراژ
۴. دستگاه‌ها (بارکد یا دستی) و حاضرین
۵. تصاویر، ویدیو و شرح بازدید

هر مرحله به‌صورت خودکار ذخیره می‌شود. کاربر می‌تواند وسط کار خارج شود و بعداً ادامه دهد.

### شرط خروج از صف در دست اقدام
پرونده تنها وقتی «بازدید شده» می‌شود که این‌ها تکمیل باشند:
- مختصات GIS ثبت‌شده
- حداقل یک تصویر
- آمپراژ کنتور
- شرح بازدید غیرخالی

اگر چیزی کم باشد، برنامه دقیقاً فهرست موارد ناقص را نشان می‌دهد و اجازه تغییر وضعیت نمی‌دهد.

### اصول طراحی
- فونت وزیرمتن، جهت راست‌به‌چپ در کل برنامه
- اعداد فارسی در نمایش، اعداد لاتین در ذخیره‌سازی
- تاریخ شمسی همه‌جا در رابط کاربری، یونیکس در پایگاه داده
- دکمه‌های بزرگ — کاربر با دستکش و زیر آفتاب کار می‌کند
- حالت روشن با کنتراست بالا به عنوان پیش‌فرض
- هیچ عملیاتی بیش از دو ضربه فاصله نداشته باشد

---

## ۹. مدارک پیوست پس از بازدید

دسته‌بندی پیوست‌ها (`attachments.category`):

| کد | دسته |
|---|---|
| ۰ | لاگ ماینرها |
| ۱ | نتایج تحلیل ماینرها |
| ۲ | صورتجلسه کمیسیون رمزارز |
| ۳ | صورتحساب انرژی |
| ۴ | صورتحساب خسارت |
| ۵ | نامه اقدام به واحد فروش |
| ۶ | نامه اقدام به واحد حقوقی |
| ۷ | نامه به برق شهرستان جهت تعویض |
| ۸ | سایر |

پیوست‌ها بعد از بازدید و حتی پس از بایگانی شدن پرونده قابل افزودن هستند.

---

## ۱۰. ارسال گزینشی مدارک به واحدها

جریان کار:
۱. انتخاب پرونده
۲. انتخاب واحد مقصد: فروش / حراست / حقوقی / برق شهرستان
۳. انتخاب موارد با چک‌باکس — فرم گزارش، تصاویر (تک‌تک)، پیوست‌ها (تک‌تک)
۴. افزودن توضیح اختصاصی برای آن ارسال
۵. انتخاب قالب خروجی: PDF یا Word
۶. تولید فایل و سپس دانلود یا اشتراک‌گذاری

اشتراک‌گذاری در اندروید با `Intent.ACTION_SEND` انجام می‌شود که واتساپ، تلگرام، ایتا، بله، ایمیل و بلوتوث را پوشش می‌دهد. هیچ ادغام اختصاصی با هیچ شبکه اجتماعی لازم نیست.

هر ارسال در جدول `dispatches` ثبت می‌شود تا سابقه بماند که چه چیزی، چه زمانی، به کدام واحد رفته است.

---

## ۱۱. گزارش‌گیری و آمار

خروجی اکسل با فیلترهای: بازه تاریخ شمسی، شهرستان، نوع گزارش، وضعیت، کارشناس.

ستون‌های فایل اکسل: کد رهگیری، نوع گزارش، تاریخ گزارش، تاریخ بازدید، شهرستان، بخش، آدرس، شماره اشتراک، شماره پرونده، شماره قبض، نام مالک، آمپراژ کنتور، آمپراژ مصرفی، تعداد دستگاه، مجموع توان، وضعیت، کارشناس، مختصات.

صفحه آمار در اپ: تعداد کل، در دست اقدام، بازدید شده، تفکیک بر اساس نوع گزارش، تفکیک بر اساس شهرستان، مجموع توان کشف‌شده.

---

## ۱۲. پروتکل همگام‌سازی

### اصل طراحی
اپ اندروید هرگز مستقیم با «ویندوز» حرف نمی‌زند. با یک **آدرس مقصد** حرف می‌زند که در تنظیمات قابل تغییر است. امروز آن مقصد کامپیوتر مرکزی است، فردا می‌تواند سرور باشد — بدون تغییر کد.

### انتقال یک‌طرفه و افزایشی
گوشی می‌فرستد، ویندوز دریافت و ادغام می‌کند. فقط پرونده‌هایی که `updated_at > synced_at` دارند ارسال می‌شوند.

### سه مسیر انتقال، یک پروتکل

**وای‌فای:** گوشی NanoHTTPD را روی پورت ۸۷۶۵ بالا می‌آورد. ویندوز به `http://<آی‌پی گوشی>:8765` وصل می‌شود.

**کابل USB:** با `adb forward tcp:8765 tcp:8765` همان مسیر HTTP روی کابل کار می‌کند. هیچ کد جدیدی لازم نیست.

**بسته آفلاین:** خروجی فایل `.cvz` — یک زیپ شامل `data.json` و پوشه `media/`. وقتی هیچ اتصالی برقرار نشد، فایل با کابل یا حافظه جانبی منتقل و در ویندوز وارد می‌شود.

### نقاط انتهایی HTTP
```
GET  /ping                 → شناسه دستگاه، کد کارشناس، نسخه اسکیما
GET  /manifest?since=<ts>  → فهرست شناسه‌ها و updated_at های تغییریافته
GET  /report/<id>          → JSON کامل پرونده با فهرست رسانه‌ها
GET  /media/<id>           → فایل خام
POST /ack                  → تأیید دریافت، گوشی synced_at را به‌روز می‌کند
```

احراز هویت با یک کد شش‌رقمی که هنگام شروع همگام‌سازی روی گوشی نمایش داده و در ویندوز وارد می‌شود.

### چرا UUID
با چند کارشناس، شماره ترتیبی حتماً تکراری می‌شود و پرونده‌ها در آرشیو مرکزی روی هم می‌افتند. UUID این مشکل را از ریشه حذف می‌کند. کد رهگیری برای انسان است، `id` برای ماشین.

---

## ۱۳. رسانه

- تصاویر: حداکثر ضلع ۱۹۲۰ پیکسل، JPEG کیفیت ۸۵
- ویدیو: رزولوشن ۷۲۰p، حداکثر ۹۰ ثانیه برای هر کلیپ
- روی هر تصویر به صورت خودکار درج می‌شود: تاریخ و ساعت شمسی، مختصات، کد رهگیری، کد کارشناس
- ذخیره در حافظه داخلی اپ، نه در گالری عمومی

---

## ۱۴. امنیت

داده‌های مالک، کد ملی، و نام پرسنل حراست و پلیس امنیت حساس هستند:

- پایگاه داده با SQLCipher رمزنگاری شود
- ورود به اپ با PIN یا اثر انگشت
- فایل‌های رسانه در حافظه خصوصی اپ، خارج از دسترس گالری
- بسته `.cvz` با رمز محافظت شود
- `android:allowBackup="false"` تا داده به پشتیبان ابری نرود

---

## ۱۵. مراحل ساخت

**فاز ۱ — پایه:** پروژه، تم و فونت فارسی، Room و اسکیما، تاریخ شمسی، تنظیمات ✅

**فاز ۲ — چرخه اصلی:** ثبت سریع، ساخت کد رهگیری، صفحه در دست اقدام، تکمیل میدانی پنج‌مرحله‌ای، GPS، دوربین و بارکدخوان ✅

**فاز ۳ — خروجی:** تولید PDF و Word، خروجی اکسل، اشتراک‌گذاری ✅

**فاز ۴ — مدارک:** پیوست پس از بازدید، ارسال گزینشی به واحدها، بایگانی و جستجو ✅

**فاز ۵ — همگام‌سازی:** وب‌سرور روی گوشی، بسته `.cvz` ✅

**فاز ۶ — ویندوز:** WPF، دریافت‌کننده، آرشیو مرکزی، گزارش‌گیری تجمیعی ⬜ (هنوز ساخته نشده)

---

## ۱۶. قواعد کدنویسی

- هر فایل حداکثر ۳۰۰ خط
- بدون کتابخانه‌ای که حجم APK را بیش از ۲ مگابایت افزایش دهد، مگر با دلیل روشن
- مسیر فایل‌ها همیشه نسبی ذخیره شود، هرگز مطلق
- هیچ رشته فارسی داخل کد نباشد — همه در `strings.xml`
- تاریخ در پایگاه داده همیشه یونیکس میلی‌ثانیه، تبدیل به شمسی فقط در لایه نمایش
- هر تغییر اسکیما باید در مستند ویندوز هم منعکس شود (`windows/SCHEMA.md`)
